{
  "name": "amigo-api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "clean": "rm -rf ./node_modules",
    "start": "pm2 start -x $APP --name='amigo-api' --no-daemon",
    "dev": "pm2 start -x $APP --name='amigo-api' --no-daemon --watch",
    "test": "mocha --recursive --timeout 10000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.5.0",
    "body-parser": "^1.17.2",
    "express": "^4.15.3",
    "pg-promise": "^5.7.1"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-http": "^3.0.0",
    "mocha": "^3.4.1"
  }
}
